import { Box } from "@mui/material";
import { styled } from "styled-components";

export const Styled = {
  Wrapper: styled(Box)`
    height: 100%;
    width: auto;
    display: flex;
    justify-content: flex-end;
    align-items: center;
    margin: auto 8px;
  `,
};
