import { ButtonBase } from "@mui/material";
import { styled } from "styled-components";

export const Styled = {
  Wrapper: styled(ButtonBase)`
    height: 100%;
    width: 180px;
    padding: 0rem 0.5rem;
    display: grid;
    place-items: center;
  `,
};
